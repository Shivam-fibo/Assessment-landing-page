"use client";

import React from "react";

export default function StepLeftContent(): React.ReactElement {
  return (
    <div className="flex flex-col gap-3">
      <p className="text-base sm:text-lg text-slate-600 leading-relaxed font-normal max-w-md">
        Automate exam creation, enforce real-time session security, monitor live tests, and deliver instant analytical reports in one seamless platform.
      </p>
    </div>
  );
}
